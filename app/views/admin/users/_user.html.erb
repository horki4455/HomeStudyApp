<div class="col-sm-12 col-md-6 col-lg-4 mb-3">
  <div id="user-id-1">
    <div class="card">
      <%= image_tag @user.image.url, class: 'card-img-top',width: '300px', height: '200px' %>
        <div>
          <div class="card-body">
            <h4 class="card-title">
              <%= link_to @user.first_name,  admin_user_path(@user) %>
            </h4>
            <ul class="list-inline">
              <p class="card-text"><%= @user.first_name %></p>
              <li class="list-inline-item"> <i class="far fa-calendar"></i><%= I18n.l(@user.updated_at, format: :default)%></li>
            </ul>
          </div>
      　</div>
    </div>
  </div>
</div>
