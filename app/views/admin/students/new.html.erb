<div class="container">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
    <h1><%= '掲示板作成' %></h1>

    <%= form_with model: @student, url: admin_students_path, local: true do |f| %>
      <div class="form-group">
        <%= f.label :名前 %>
        <%= f.text_field :name, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :科目 %>
        <%= f.text_field :subject, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.label :その他補足 %>
        <%= f.text_area :body, class: 'form-control', row: 5, style: 'height: 80px' %>
      </div>

      <%= f.file_field :image, class: 'form-control', id: :'js-preview-img' %>
      <%= image_tag @student.image.url, id: :js_img_preview %>
      <%= f.submit '作成する', class: 'btn btn-primary' %>
    <% end %>
    </div>

  </div>
</div>
