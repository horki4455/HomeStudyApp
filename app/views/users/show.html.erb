<h1>そいつのプロフィール</h1>

<ul class="users">
    <li>
      <%= @user.full_name %>
    </li>
    <%= render 'relationships/follow_button', { user: @user } %>
</ul>

<h3>フォロー一覧</h3>
  <% @user.followings.where.not(id: current_user.id).each do |user| %>
    <p>
    <%= "名前: #{user.full_name}" %>
      <%= render 'relationships/follow_button', { user: @user } %>
      <%= link_to "show", user_path(user) %>
      <%= @user.first_name %>
    </p>
  <% end %>
<h3>フォロワー一覧</h3>
  <% @user.followers.where.not(id: current_user.id).each do |user| %>
    <p>
      <%= "名前: #{user.full_name}" %>
      <%= link_to "show", user_path(user) %>
    </p>
  <% end %>
  <h3>相互フォロー関係</h3>
  <% if @user.followers ==  @user.followings %>
  '全体のうちで何かが成立している'
  <% else %>
  'ない'
  <% end %>